import styles from "@/styles/Home.module.css";

export default function RollLoader({ rollSlide }) {
  return (
    <div className={styles.rollLoadCont}>
      <div className={styles.rollLoadHead}>
        <h2> Grant Omohundro</h2>
      </div>
      <div className={styles.rollLoadDesc}>
        <h3>HTML / CSS / JS / React / Next.JS</h3>
      </div>
    </div>
  );
}
